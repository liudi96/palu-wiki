-- 数据库初始化脚本
-- 这个脚本会在PostgreSQL容器首次启动时自动执行

-- 创建数据库（如果使用默认配置，这一步可能不需要）
-- 因为我们在docker-compose.yml中已经设置了POSTGRES_DB

-- 设置时区
SET timezone = 'Asia/Shanghai';

-- 如果需要创建额外的用户或权限，可以在这里添加
-- 例如：创建只读用户等

-- 其他初始化SQL语句可以在这里添加
-- 比如创建索引、插入初始数据等

-- 注意：GORM会自动创建表结构，所以通常不需要手动创建表